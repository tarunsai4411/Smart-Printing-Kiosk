CREATE DATABASE IF NOT EXISTS print;
USE print;

-- Tables are created automatically by Hibernate (ddl-auto=update).
-- Default admin is also created automatically on first application startup.
-- Default credentials: admin / admin123
-- Change APP_ADMIN_PASSWORD before deployment.
