let statusChart,paymentChart;async function loadDashboard(){const response=await fetch(BASE_URL);if(!response.ok)return;const jobs=await response.json();const pending=jobs.filter(j=>j.status==='PENDING').length;const printing=jobs.filter(j=>j.status==='PRINTING').length;const completed=jobs.filter(j=>j.status==='COMPLETED').length;const paid=jobs.filter(j=>(j.paymentStatus||'').toUpperCase()==='PAID');const unpaid=jobs.length-paid.length;const revenue=paid.reduce((s,j)=>s+Number(j.amount||0),0);document.getElementById('totalJobs').textContent=jobs.length;document.getElementById('pendingJobs').textContent=pending;document.getElementById('completedJobs').textContent=completed;document.getElementById('revenue').textContent=`₹${revenue.toFixed(2)}`;if(statusChart)statusChart.destroy();statusChart=new Chart(document.getElementById('statusChart'),{type:'pie',data:{labels:['Pending','Printing','Completed'],datasets:[{data:[pending,printing,completed]}]}});if(paymentChart)paymentChart.destroy();paymentChart=new Chart(document.getElementById('paymentChart'),{type:'doughnut',data:{labels:['Paid','Pending'],datasets:[{data:[paid.length,unpaid]}]}});}loadDashboard();setInterval(loadDashboard,5000);
