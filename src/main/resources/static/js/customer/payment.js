window.addEventListener('DOMContentLoaded',()=>{document.getElementById('amount').textContent=`Amount: ₹${sessionStorage.getItem('amount')||0}`;document.getElementById('payBtn').addEventListener('click',createPrintJob);});
async function createPrintJob(){const job={fileName:sessionStorage.getItem('fileName'),filePath:sessionStorage.getItem('filePath'),pages:Number(sessionStorage.getItem('pages')),copies:Number(sessionStorage.getItem('copies')),printType:sessionStorage.getItem('printType'),amount:Number(sessionStorage.getItem('amount')),status:'PENDING',paymentStatus:'PAID'};if(!job.fileName||!job.filePath||job.pages<1||job.copies<1){window.location.href='/customer/upload.html';return;}const button=document.getElementById('payBtn');button.disabled=true;try{const response=await fetch(BASE_URL,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(job)});if(!response.ok)throw new Error(await response.text());const saved=await response.json();sessionStorage.setItem('jobId',saved.jobId);sessionStorage.setItem('queueNumber',saved.queueNumber);document.getElementById('rQueue').textContent=saved.queueNumber;document.getElementById('rFile').textContent=saved.fileName;document.getElementById('rAmount').textContent=saved.amount;document.getElementById('rStatus').textContent=saved.status;new bootstrap.Modal(document.getElementById('receiptModal')).show();setTimeout(()=>window.location.href='/customer/track.html',3000);}catch(error){console.error(error);document.getElementById('message').className='mt-3 text-danger';document.getElementById('message').textContent='Unable to create print job.';button.disabled=false;}}
